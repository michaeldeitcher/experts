- content_for :header do
  %h1
    Experts

= image_tag('hello_expert.jpg')

- content_for :footer do
  - if current_user
    = link_to('Logout', destroy_user_session_path, :method => :delete)
  - else
    = link_to('Login', new_user_session_path, 'data-rel'=>"dialog")